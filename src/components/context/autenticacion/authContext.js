import {createContext} from 'react'

const authContext=createContext();

export default authContext;

//en todo context estan disponibles todas las funciones de un state, en este caso en authContoext estasn dispnible las funciones de authSate